---
import { textFieldInputClass } from "./TextField.recipes";
import type { ComponentVariantAttributes } from "@components/utils/twCva";

type Props = ComponentVariantAttributes<"input", typeof textFieldInputClass>;

const { color, size, variant, width, ...props } = Astro.props;
---

<input
  class={textFieldInputClass({ color, size, variant, width })}
  {...props}
/>
