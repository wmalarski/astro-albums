---
import { textFieldDescription } from "./TextField.recipes";
import type { ComponentVariantAttributes } from "@components/utils/twCva";

type Props = ComponentVariantAttributes<"span", typeof textFieldDescription>;

const { class: className, ...props } = Astro.props;
---

<span class={textFieldDescription({ className })} {...props}>
  <slot />
</span>
