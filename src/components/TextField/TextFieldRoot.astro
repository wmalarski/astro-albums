---
import { textFieldRootClass } from "./TextField.recipes";
import type { ComponentVariantAttributes } from "@components/utils/twCva";

type Props = ComponentVariantAttributes<"fieldset", typeof textFieldRootClass>;

const props = Astro.props;
---

<fieldset class={textFieldRootClass()} {...props}>
  <slot />
</fieldset>
