---
export type Props = {
  covers?: string[] | undefined;
  header: string;
};

const { covers, header } = Astro.props;

const showCover = (covers || []).length > 0;
---

{
  showCover && (
    <div class="carousel rounded-box">
      {covers?.slice(1)?.map((cover) => (
        <div class="carousel-item">
          <img src={cover} alt={header} />
        </div>
      ))}
    </div>
  )
}
